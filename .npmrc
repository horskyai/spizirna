# capacitor-thermal-printer (BT tisk) má peer @capacitor/core ^7, my máme 8.
# legacy-peer-deps povolí instalaci i s tímto konfliktem (funguje, ověřeno).
# Nutné i pro Vercel build (jinak npm install padá na peer dependency conflict).
legacy-peer-deps=true
